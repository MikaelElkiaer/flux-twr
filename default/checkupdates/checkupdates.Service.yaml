kind: Service
apiVersion: v1
metadata:
  labels:
    app: checkupdates
  name: checkupdates
spec:
  selector:
    app: checkupdates
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: metrics
      port: 9100
      targetPort: metrics

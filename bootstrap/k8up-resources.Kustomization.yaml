apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: k8up-resources
  namespace: flux-system
spec:
  dependsOn:
    - name: k8up-controller
  interval: 10m
  path: ./backup/k8up-resources
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: backup
  wait: true

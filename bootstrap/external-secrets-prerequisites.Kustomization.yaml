apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-secrets-prerequisites
  namespace: flux-system
spec:
  dependsOn:
    - name: cert-manager-controller
  interval: 10m
  path: ./kube-system/external-secrets-prerequisites
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true

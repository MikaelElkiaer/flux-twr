apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: reloader
  namespace: flux-system
spec:
  interval: 10m
  path: ./reloader
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: reloader
  wait: true

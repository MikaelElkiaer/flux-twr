apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: prom-stack
  name: speedtest
  namespace: default
spec:
  selector:
    matchLabels:
      app: speedtest
  endpoints:
    - port: http
  namespaceSelector:
    matchNames:
      - default

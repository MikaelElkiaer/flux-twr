apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: k8sup
spec:
  interval: 10m
  chart:
    spec:
      chart: k8sup
      version: 4.9.0
      sourceRef:
        kind: HelmRepository
        name: k8sup
        namespace: backup
  upgrade:
    crds: CreateReplace
  values:
    k8up:
      envVars:
        - name: BACKUP_SKIP_WITHOUT_ANNOTATION
          value: "true"
